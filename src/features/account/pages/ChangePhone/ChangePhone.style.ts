import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  cancelSpan:{
    fontWeight: 400,
    fontSize: '9.619px',
    lineHeight: '11.72px',
    transform: 'scale(1.001, 1.09)',
  },
});
